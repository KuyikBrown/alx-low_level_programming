#include <stdio.h>

/**
 * the code is to prints all single digit numbers of base 10 starting from 0, followed by a new line.
 */

int main(void)
{
	int i;
	for (i = 49; i < 50; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}	
